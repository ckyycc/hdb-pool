

# Language
language: node_js
node_js:
  - "node"

# Branches to build
branches:
  only:
    - master
    - test

# Build
script:
  - npm install mocha
  - npm install nyc
  - npm install should
  - npm install sinon
  - npm test

after_success:
  - npm run coverage

# Notifications
notifications:
  email:
    recipients:
      - chengkuang@gmail.com
    on_success: change
    on_failure: change
